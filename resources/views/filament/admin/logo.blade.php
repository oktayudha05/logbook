<div class="flex items-center gap-3">
  <img src="{{ asset('logo.svg') }}" alt="Logo" class="h-10">
  <div class="flex flex-col">
    <span class="text-xl font-bold">{{ config('app.name', 'Company') }}</span>
    <span class="text-sm opacity-50">{{ config('app.tagline', 'Manajemen Panel') }}</span>
  </div>
</div>
